{
  "name": "samee",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev-server": "nodemon server --watch server",
    "dev-client": "browser-sync start --proxy http://localhost:8005 --files client",
    "deploy-local": "open http://localhost:8005 && node server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.5.0",
    "module-alias": "^2.0.1",
    "simple-ssh": "^1.0.0"
  },
  "_moduleAliases": {
    "@config": "./config.js"
  }
}
